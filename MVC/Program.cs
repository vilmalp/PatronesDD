using System;
/*
 Patrón MVC - Model View Controler
 Consiste en separar según el contexto. En la capa modelo se representa el modelo de datos.
 En la capa vista está todo lo que el cliente va a visualizar y utiliza el modelo para representar los datos.
 La capa controlador actúa como un intermediario entre la capa vista y modelo para mantenerlos separados.

     */
namespace MVC
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
}
